Ensure that the dataset loaders are marked as "download = True", if you have problems with download speed as I have, I included the files which would be downloaded in the data/cifar-100-python folder.
Make sure that if you add these files, you place them inside "./data/cifar-100-python" as that is where the dataset loader looks for them. Add all the files (train,test,meta)
